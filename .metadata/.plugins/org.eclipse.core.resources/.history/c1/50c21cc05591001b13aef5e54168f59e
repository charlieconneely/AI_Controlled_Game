package ie.gmit.sw.ai;

public class Hunter implements Command {

	private GameModel model;
	private char enemyID;
	private int row, col;
	
	public Hunter(GameModel model, char enemyID, int row, int col) {
		this.model = model;
		this.enemyID = enemyID;
		this.row = row;
		this.col = col;
	}
	
	@Override
	public boolean execute() {
		if (isTouchingPlayer()) return false;
		return false;
	}
	
	private boolean isTouchingPlayer() {
		if (model.get(row - 1, col) == '1' || model.get(row + 1, col) == '1'
			|| model.get(row, col - 1) == '1' || model.get(row, col + 1) == '1') {
			return true;
		}
		return false;
	}
}
